'use client'

import { Card } from '@/components/Card'
import { Trophy, Medal, Award, Crown, Star } from 'lucide-react'
import { useAuth } from '@/contexts/AuthContext'
import { localStorageService } from '@/lib/localStorage'

export default function LeaderboardPage() {
  const { user } = useAuth()

  // Generate local leaderboard data
  const generateLocalLeaderboard = () => {
          ))}
        </div>

        {user && (
          <div className="text-center mt-8">
            <div className="bg-white/10 backdrop-blur-sm rounded-xl p-6 border border-white/20 max-w-md mx-auto">
              <h3 className="text-lg font-semibold mb-2">Your Total Score</h3>
              <p className="text-3xl font-bold text-purple-300">
                {localStorageService.getGameScores(user.id).reduce((sum, score) => sum + score.score, 0).toLocaleString()}
              </p>
              <p className="text-sm text-blue-200">Keep playing to climb the ranks!</p>
            </div>
          </div>
        )}
      </div>
    </main>
  )
}
